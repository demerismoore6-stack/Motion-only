import express from 'express';\nimport cors from 'cors';\nimport dotenv from 'dotenv';\n\ndotenv.config();\n\nconst app = express();\nconst PORT = process.env.API_PORT || 3001;\n\napp.use(cors());\napp.use(express.json());\n\napp.get('/health', (req, res) => {\n  res.json({ status: 'ok' });\n});\n\napp.get('/api', (req, res) => {\n  res.json({\n    name: 'Motion Only API',\n    version: '0.1.0'\n  });\n});\n\napp.listen(PORT, () => {\n  console.log(`🎵 Motion Only API running on http://localhost:${PORT}`);\n});\n